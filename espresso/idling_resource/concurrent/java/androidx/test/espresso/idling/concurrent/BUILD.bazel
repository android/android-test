# Description:
#   Light weight module that contains IdlinResources around concurrent Java APIs

load("//build_extensions:axt_android_aar.bzl", "axt_android_aar")
load("//build_extensions:maven_artifact.bzl", "maven_artifact")
load("//build_extensions:axt_versions.bzl", "ESPRESSO_IDLING_CONCURRENT_ARTIFACT")
load("//build_extensions:combine_jars.bzl", "combine_jars")

package(
    default_visibility = ["//:__subpackages__"],
)

licenses(["notice"])

android_library(
    name = "scheduled_thread_pool_idling_resource",
    srcs = [
        "IdlingScheduledThreadPoolExecutor.java",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "//espresso/idling_resource/java/androidx/test/espresso:idling_resource_internal",
    ],
)

android_library(
    name = "thread_pool_idling_resource",
    srcs = [
        "IdlingThreadPoolExecutor.java",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "//espresso/idling_resource/java/androidx/test/espresso:idling_resource_internal",
    ],
)

android_library(
    name = "idling_concurrent",
    srcs = [
        "IdlingScheduledThreadPoolExecutor.java",
        "IdlingThreadPoolExecutor.java",
    ],
    manifest = "AndroidManifest.xml",
    tags = [
        "maven_coordinates=%s" % ESPRESSO_IDLING_CONCURRENT_ARTIFACT,
    ],
    deps = [
        "//espresso/idling_resource/java/androidx/test/espresso:idling_resource_internal",
    ],
)

# rule for outputting externally released binary.
axt_android_aar(
    name = "idling_concurrent_release",
    included_dep = ":idling_concurrent",
)

maven_artifact(
    name = "idling_concurrent_maven_artifact",
    last_updated = "20170622000000",
    target = ":idling_concurrent_release",
)
